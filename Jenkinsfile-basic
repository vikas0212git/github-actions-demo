pipeline {
  agent any
  stages {
    stage('verify act installation') {
      steps {
        bat 'act --version'
      }
    }
    stage('run the entire pipeline') {
      steps {
        bat 'act'
      }
    }
    stage('view the executive graph') {
      steps {
        bat 'act -l'
      }
    }
  }
}
